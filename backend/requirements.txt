# Backend service (reduced-order model + API). Runs on plain CPU.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
numpy>=1.26

# Full pipeline (segmentation + FSI) — heavier, GPU/conda recommended:
#   torch>=2.2  monai>=1.3          (3D U-Net segmentation)
#   pydicom>=2.4  SimpleITK>=2.3    (DICOM ingest/anonymize)
#   pyvista>=0.43  vtk>=9.3         (meshing)
#   fenics-dolfinx                  (FSI solver; install via conda-forge or
#                                    the dolfinx/dolfinx:stable Docker image)
